name: Trigger auto deployment for contapp-talkfloripa-github

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/contapp-talkfloripa-github-AutoDeployTrigger-c2d0f80e-abce-40cf-b577-00e234ed0070.yml'

  # Allow manual trigger 
  workflow_dispatch:
      
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.CONTAPPTALKFLORIPAGITHUB_AZURE_CREDENTIALS }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v1
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: ca5b85d3f3daacr.azurecr.io
          registryUsername: ${{ secrets.CONTAPPTALKFLORIPAGITHUB_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAPPTALKFLORIPAGITHUB_REGISTRY_PASSWORD }}
          containerAppName: contapp-talkfloripa-github
          resourceGroup: rg-talkfloripa2023-brazilsouth-002
          imageToBuild: ca5b85d3f3daacr.azurecr.io/contapp-talkfloripa-github:${{ github.sha }}
          

     

